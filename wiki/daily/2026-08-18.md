# 2026-08-18

## v0.4.0 promoted to main

Taylor's go-ahead at 17:49. Pre-promotion additions: gradient card
seeds as the default image on first run (flag survives deletion; wipe
re-seeds) and a two-step "Reset all settings" (preferences only —
devices/media kept). Gates: typecheck/lint/102 tests, production
export build, and the fresh-state smoke test on a clean origin served
from the export (boots, seeds the gradient card, onboards — no v0.3.0
class crash). Merged --no-ff to main, tagged v0.4.0, Pages deploy
green in 64 s. Day's review loop also shipped: rail to top-center,
top-right view actions, pointer-guard fixes (SVG targets + portal
bubbling), inspector trim + selection pinning, cursor→box-anchored
text hover cards, dot·arcmin report rows, projection-line overlay
fix, 3D click-vs-drag threshold, units spacing sweep, bigger 3D hover
zones.

## v0.5.0 promoted (23:59 go-ahead)

Late-evening loop → main: 3D-first boot (instant orbit pose;
hydration-gated ViewStack fixes the stored-2D phantom exit sequence),
seeded default image with persisted active selection, arrow-key
library nav, 860×860 workbench defaults, icon device-row readouts
(distance first), destructive settings cluster behind confirm
dialogs. Smoke-tested both boot paths on the production export
(fresh→3D orbit; stored-2D→2D only). Tagged v0.5.0; Pages deploy
green in 64 s.
