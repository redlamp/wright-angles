# 2026-08-19

## Morning review loop (Taylor live, 00:09–00:50)

Fresh-visitor defaults (device set spanning Switch 2 → 120″ projector,
closed panels, couch/handheld/unlinked), pivoted aspect labels (9:16),
Device Manager slimming (rows = eye + color-named label; color picker
as a button on the editor title; steppers and kebab gone), selection
unified onto the app-wide store (rows ↔ 2D/3D ↔ inspector), screens
kept double-sided with the U-mirror now applied before first paint
(the sometimes-flipped-image fix), and three features parked behind
lib/flags.ts (3D bodies, pinned devices, column collapse) — tracked in
GitHub issue #1.

- created [[plan-per-device-crop]] — device-by-device cropping design
  (two-layer model over the existing media crop; build later).

## Release + pickup session (autonomous, after 00:44 go-ahead)

- Shipped the mirrored-side text-box hover (a hover catcher on BOTH
  faces of each box — pointer events deliver front-to-back, so from
  behind the media plane used to swallow the hit).
- **v0.6.0 → main** on Taylor's go-ahead: gates, export build, clean
  7842 smoke (fresh boot = instant 3D, starter devices, seeded
  gradient), Pages deploy green.
- Filed issue #2 (Taylor): auto-OCR images on import, debounced, plus
  the need for a timeline-OCR plan.
- **Built [[plan-per-device-crop]]** on feature/per-device-crop →
  dev: deviceCrops overrides across 2D/3D/report/export/loupe, crop
  scope UI riding app-wide selection, per-distinct-crop texture
  clones in 3D, override pruning on device delete, 6 new tests (108
  total). Open questions resolved by the plan's defaults — see the
  note's Built section for what Taylor should review.
