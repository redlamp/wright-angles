# 2026-08-17

## v0.3.0 shipped

Taylor's go-ahead just after midnight: dev → main (`--no-ff`), tagged
v0.3.0, Pages deploy green. Everything since v0.2.0: comparison table,
sub-retina flags, crop tool, local OCR pipeline, playback engine with
transport, the whole distance-label registration system, drag handles +
grab cursors, shaded mannequin, stance × input matrix with standing
desk, instant height slider, label drop shadows.

## Post-ship tweaks (dev)

- Stance options reordered Standing · On a couch · At a desk so the two
  option rows pair by column with Handheld · Gamepad · Mouse & KB.
- Figma-style link toggle spanning both HUD rows: locks stance and
  input through the column pairing (engaging it snaps input to the
  current stance's pair). Off by default.

## v0.3.1 hotfix (morning)

Production crashed for every FRESH visitor: an inline object fallback
in a zustand selector (DetailFlyout panel position) minted a new
reference per getSnapshot → React #185 loop. Every dev machine had
stored positions and never hit it. Fixed via module-constant fallback
on `fix/fresh-state-selector-loop`, shipped as v0.3.1; fresh-state
smoke test added to promotion gates. Feature-branch discipline starts
here.

## Morning review Q&A

Taylor's big review triaged into the renumbered plan; decisions filed:

- created [[qa-2026-08-17-media-debug-decisions]]

## Midday build run (Taylor stepping in and out)

Shipped through feature branches onto dev: hotkeys + cheat sheet +
LMB select/pan (4.x), type-scale wave 1 (2.2, Tailwind tokens only),
crop v2 aspect redesign (agent-built, 5.4), active-media layout
(5.1–5.3), distance units + imperial ft/in format, media-panel batch
(5.7: scan boxes on the media, eye toggle, resizable list, header
note in the title bar, fixed two columns, split scrolling), OCR
keyframes for timeline media (topic 9, lib/scan-keyframes +
frame-capture), OCR text grouping with descender-aware sizes and
group tints (7.1–7.3, lib/text-groups), labeled measure boxes in the
report (6.4), scan follow-through links (5.5). Tests 52 → 73.
Chrome's extension dropped mid-run, so 9.x and the late media-panel
work are gate-verified only — the plan marks what needs eyes.

## Next

- created [[test-plan-2026-08-17-v3-media-debug]] — v0.3.0 acceptance +
  the media-content debug track (staged OCR overlays → legibility
  matrix → video frame scanning → contrast/loupe/safe-areas), plus
  carry-over backlog and open questions for Taylor.

## Autonomous queue run (Taylor away)

Worked the queue solo, all gated (typecheck/lint/tests, 73 → 80):

- `feature/hover-and-split` → dev: OCR lines now split at horizontal
  word gaps > 2× line height (fixes two-columns-read-as-one-line;
  `splitWordsAtGaps`/`wordsBbox` in lib/ocr with tests), and 2D text
  boxes on This Device get a hover card (label, source px height,
  worst arcmin + verdict, group swatch).
- `feature/workbench-polish` → dev, four commits:
  - Tab order Media Library · Perception Report · Device Manager
    (strip + sidebar rail); workbench defaults to Media.
  - Library defaults to list view — bigger thumbs, names only,
    resolution dropped from rows; sort dropdown replaced by a lucide
    icon group (grip / clock-down / clock-up / A-Z).
  - New `SplitGrid`: all three tabs share ONE persisted adjustable
    first-column width (Taylor left the shared-vs-per-tab call to me;
    shared won for parity) plus a divider collapse toggle that folds
    the first column away entirely.
  - Floating panels clamp ≥ min(240px, width) into view at mount and
    window resize (5120→2560 case) — never during drag, so deliberate
    edge-tucking still works.

Browser-verified in the live tab until Taylor returned mid-check (theme
+ window changed under me); backed off. His parked 2D/3D view may have
been flipped once by my sidebar clicks — mention on handoff.

## Evening review round + overnight run kickoff

Taylor's live review (22:29–22:52) → shipped same-hour: crop as
dropdown + reference size below it (5.6 width fix), SplitGrid height
regression fix (report details flush again), scroll-to-selected in the
detection list, keyframe hover + prev/next transport buttons, the 3.x
click-to-inspect device inspector, color-vision simulation (10.5,
relocated from the inspector to the 2D/3D view toolbars on feedback),
select popups sized to content, browser-zoom warning chip (11.1).

Overnight (Taylor reviews in the morning): created
[[test-plan-2026-08-18-overnight-run]]. Built the rest of topic 10
behind an "overlays" toolbar menu — SMPTE safe areas (10.3), WCAG
contrast badges with Otsu text/background estimation (10.1, new
lib/contrast), sub-acuity stroke warnings (10.4, stroke ≈ cap/7), and
the 8× pixel loupe (10.2). Two worktree agents built in parallel:
credit-card calibration (11.2, lib/calibration + Settings dialog) and
import hygiene (11.5: EXIF strip via canvas re-encode + log-scale
distance slider). 11.3 confirmed already shipped as 3D outlines; 6.5
baseline-beside-selection spec strip added to the report. Tests
73 → 102. All merged --no-ff into dev, pushed.
